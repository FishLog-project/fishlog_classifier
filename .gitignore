# 데이터셋 원본은 재배포 금지(A.6) — 절대 커밋하지 않는다
data/raw/
data/clean/
data/splits/
data/reject/
data/review/
# Colab 업로드용 패키징 산출물
data/*.zip
# 품질 채점 캐시 — score_quality.py로 언제든 재생성된다
data/quality_scores.csv
# dedup이 매번 다시 만드는 파생 파일 (licenses.csv·rejected.txt는 커밋한다 — 출처 근거/검수 결과)
data/dup_groups.csv
data/clean_manifest.csv
data/review_manifest.csv
!data/**/.gitkeep

# 산출물
models/*.pt
models/*.csv
server/model.onnx
reports/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# API 키 (네이버 검색 API 등) — 절대 커밋 금지
.env

# OS/IDE
.DS_Store
Thumbs.db
.vscode/
.idea/
