# 로드맵 — 지금 뭘 할 차례인가

> 이 파일이 **진행 상황의 단일 기준**이다. 작업을 끝낼 때마다 체크박스와 "현재 위치"를 갱신한다.

## 현재 위치 (2026-08-18 기준)

**Phase 2 완료 → Phase 3(학습) 착수.** 데이터 6,999장 분할 완료 (train 4,904 / val 1,048 / test 1,047).

- 있는 것: `src/config.py`(25클래스), `src/dataset.py`, `src/train.py`,
  `scripts/*` 8종(수집 3 + 정제 3 + 검수 보조 2), **분할된 데이터 6,999장**, `server/labels.json`
- 없는 것: `src/evaluate.py`, `src/export_onnx.py`, `server/main.py`·`inference.py`, `Dockerfile`
- 확정: 25클래스(어종 24 + `기타`) / MVP 종당 250장 / 학습은 Colab T4 ([decisions.md](decisions.md) A-10~12)
- 미결정: 공모전 마감일, 배포처, 24종 밖 어종 앱 UX ([decisions.md](decisions.md) B섹션)

### 데이터 현황 (2026-08-18 실측)

| | 장수 |
|---|---|
| train / val / test | 4,904 / 1,048 / 1,047 (합 **6,999**) |
| 소스 | iNaturalist 약 4,900 + 검색(DDG·Bing) 약 2,100 |
| 수집 원본 `data/raw` | 9,005 → 정제 후 `data/clean` 6,999 |
| 격리분 | 1,901장 (자동 필터 `data/reject` 1,221 + 사람 검수 `data/review` 680) + 중복 제거 약 105 |

**목표 미달 3종**: 볼락 146, 삼치 151, 전갱이 189 (다음으로 적은 종은 동자개 216).
iNat 관측 수 자체가 적고(볼락 39건), 검색 결과는 어시장·조리 사진이 대부분이라 검수에서 60~80% 탈락했다.
→ A-11 방침대로 **이대로 1차 학습**하고, 평가에서 이 3종의 recall을 보고 보강 여부를 정한다.

### ▶ 다음에 할 일 (Phase 3)

Phase 3 사전 준비는 2026-08-18에 끝났다:
- [x] `python -m src.dataset --check` — 4,904 / 1,048 / 1,047 확인
- [x] `python -m src.dataset --preview` — `reports/aug_preview.jpg` 육안 확인 (증강 정상)
- [x] `python -m scripts.package_data` — `data/splits.zip` (1,067 MB, 한글 경로 UTF-8 검증 통과)
- [x] `python -m src.train --smoke` — 2단계 파인튜닝·체크포인트 저장까지 통과 (스모크 산출물은 삭제함)

1차 학습도 끝났다 (val_top3 0.8721, 아래 "베이스라인 결과" 참조).

### ▶ 다음에 할 일 (Phase 4 — 진단)

베이스라인이 합격선에 -2.8pp 미달이고 **과적합이 확실**하다. 튜닝을 더 하기 전에
"어디서 틀리는가"를 먼저 봐야 한다 ([evaluation.md](evaluation.md) "목표 미달 시 대응 순서" 1번).

1. `src/evaluate.py` 작성 → test 1,047장으로 산출물 5종 ([evaluation.md](evaluation.md) "산출물")
2. **종별 recall** — 볼락·삼치·전갱이·동자개(데이터 부족 4종)가 무너졌는지 확인
3. **혼동행렬** — 6그룹이 *대칭* 혼동이면 데이터 부족, *한쪽 쏠림*이면 라벨 오염
4. **`worst_cases/`** — 확신했는데 틀린 50장. 어시장·조리 사진이 나오면 라벨 오염 확진
5. 진단 결과에 따라 분기:
   - 라벨 오염 → 검수 2차 후 재학습
   - 데이터 부족 → 해당 종 보강 수집
   - 둘 다 아니면 → 정규화 강화(mixup/cutmix) 또는 `convnext_tiny`

상세 → [training.md](training.md) / [data-pipeline.md](data-pipeline.md)

---

## Phase 체크리스트

### Phase 1 — 환경·레포·config ✅ (완료)
- [x] 폴더 구조 생성 (`data/{raw,clean,splits}/<24종>`, `src/`, `server/`, `models/`, `reports/`, `scripts/`)
- [x] `requirements.txt` / `server/requirements.txt`(슬림 배포용)
- [x] `src/config.py` — 24종 고정 순서 + 학명·키워드·난이도·목표량
- [x] `src/dataset.py` — Dataset/증강/불균형 샘플러/점검 CLI
- [x] `src/train.py` — 2단계 파인튜닝 루프
- [x] `README.md`, `.gitignore`, `CLAUDE.md`, `docs/`
- [x] **Python 3.11 venv + 의존성 설치** (3.11.9 / torch 2.13.0+cpu / timm 1.0.28 / albumentations 2.0.8)
      `stringzilla==5.1.1` 핀 필요 — 5.1.2는 Windows cp311 휠이 없어 MSVC 빌드를 요구한다
- [x] `python -m src.train --smoke` 로 파이프라인 검증 통과 (2단계 파인튜닝·체크포인트·한글 경로 OK)

### Phase 2 — 데이터 수집·정제·분할 ✅ (완료, 2026-08-14)
- [x] `scripts/crawl_inat.py` — iNaturalist API (25클래스 taxon 매칭 확인, 학명 3건 정정)
- [x] `scripts/crawl_ddg.py` — **DuckDuckGo 검색 (실사용 사진 주 소스)**
- [x] `scripts/crawl_search.py` — Bing 크롤링 (보조. 키워드당 20~60장이면 고갈)
- [x] `scripts/crawl_naver.py` — 네이버 API (미사용: 신규 앱에 '검색' API 권한이 없음)
- [x] `scripts/dedup.py` — phash 중복 제거 + 거부 목록 영구화
- [x] `scripts/prefilter.py` — ImageNet 어류 확률 필터 + `make_scorer`(크롤링 중 실시간 판정)
- [x] `scripts/review.py` — 검수 대상만 모아 보여주고 삭제 결과 확정
- [x] `scripts/refsheet.py` — 혼동 쌍 비교 참고표 (검수 보조)
- [x] `scripts/split.py` — 70/15/15 + obsid/phash 그룹 누수 방지
- [x] 사람 검수 — 혼동 쌍 web 사진 1,370장 검토 → 680장 격리(약 50%)
- [x] `기타` 클래스 250장 (4버킷 배분)
- 상세 → [data-pipeline.md](data-pipeline.md)

### Phase 3 — 학습 🔶 (베이스라인 확보, 2026-08-18)
- [x] `python -m src.dataset --preview` 로 증강 결과 눈으로 확인
- [x] 베이스라인: `efficientnet_b0`, 224px, T4에서 **16.9분** / 26 epoch에서 조기종료
- [x] `models/best.pt` 확보 (16.4MB, Drive `MyDrive/fishlog/models/`)
- [x] 학습 곡선 확인 → **과적합 확진**
- [ ] (선택) `convnext_tiny` 비교 — Phase 4 진단 후 판단
- 상세 → [training.md](training.md)

#### 베이스라인 결과 (val 1,048장)

| 지표 | 결과 | 합격 기준 | 판정 |
|---|---|---|---|
| val Top-3 | **0.8721** (E18) | 0.90 | ❌ -2.8pp |
| val Top-1 | 0.7538 (E25) | 0.75 | ✅ |

**과적합.** train_top1 0.9895 vs val_top1 0.7538 → 간격 24pp.
epoch 10 이후 train loss만 계속 떨어지고 val loss는 1.42에서 평평 → 조기종료(patience 8) 정당.
**더 오래 돌려도 오르지 않는다.** epoch/LR 튜닝이 아니라 데이터 쪽 문제다.

원인 후보 3가지는 현재 데이터로 구분 불가 → Phase 4 진단이 먼저다:
1. 데이터 부족 (종당 250장은 25클래스 구분에 빠듯)
2. **라벨 오염** (웹 크롤링분에 어시장·조리 사진 혼입)
3. 혼동 쌍 6그룹의 본질적 난이도

### Phase 4 — 평가 ⬜ (1일)
- [ ] `src/evaluate.py` 작성 (미작성)
- [ ] test셋 Top-1 / Top-3, per-class precision·recall
- [ ] 혼동행렬 → 혼동 그룹 6쌍 집중 점검
- [ ] 목표 미달 종 데이터 보강 후 재학습
- 상세 → [evaluation.md](evaluation.md)

### Phase 5 — ONNX Export ⬜ (0.5일)
- [ ] `src/export_onnx.py` 작성 (미작성)
- [ ] `.pt` vs `.onnx` 출력 수치 일치 검증 (max abs diff < 1e-4)
- 상세 → [serving.md](serving.md)

### Phase 6 — FastAPI + Docker ⬜ (2일)
- [ ] `server/inference.py`, `server/main.py`
- [ ] `uncertain` 임계값 튜닝
- [ ] `Dockerfile` 빌드·실행
- [ ] 배포 (호스팅 미정 → decisions.md B-2)
- 상세 → [serving.md](serving.md)

### Phase 7 — 앱 연동 ⬜
- [ ] 백엔드 ↔ 모델 서버 계약 확정, 엣지케이스 테스트
- [ ] 사용자 확정 결과 로깅 → 재학습 데이터 축적
- 상세 → [integration.md](integration.md)

---

## 2주 타임라인 (계획서 기준)

| 일차 | 작업 |
|---|---|
| D1 | Phase 1 + 수집 착수 |
| D2~D4 | 수집·정제·분할 |
| D5~D7 | 베이스라인 학습 |
| D8~D9 | 평가 + 보강 재학습 |
| D10 | ONNX export |
| D11~D12 | FastAPI + Docker |
| D13~D14 | 앱 연동, 임계값 튜닝, 버퍼 |

## 리스크

| 리스크 | 대응 |
|---|---|
| 수집이 가장 오래 걸림 | MVP 종당 250장으로 먼저 학습, 부족한 종만 보강 |
| 혼동 쌍 6그룹 | 데이터 넉넉히 + Top-3 서빙으로 흡수 |
| 웹 사진 vs 실사용 사진 도메인 갭 | 손에 든/바닥/젖은 사진 반드시 학습셋에 포함 |
| 24종 밖 어종·비물고기 입력 | `기타` 클래스로 흡수 (A-10). 데이터 다양성이 관건 |
| 로컬 GPU 부족(MX450 2GB) | Colab T4로 학습, 로컬은 코드 검증용 |
| Colab 세션 끊김 | `models/`를 Drive에 링크, `--resume models/last.pt` |

---

## Phase 4 진단 로그 (2026-08-18, test 1,047장 / `best.pt` epoch 17)

### 결과

| 지표 | test | 기준 | |
|---|---|---|---|
| Top-3 (25클래스) | 87.68% | 90% | ❌ -2.3pp |
| Top-1 (25클래스) | 76.98% | 75% | ✅ |
| Top-3 (어종 24종만) | 88.01% | 90% | ❌ |
| **`기타` recall** | **65.79%** | 80% | ❌ -14.2pp |
| `기타` 오탐율 | 0.99% | 10%↓ | ✅ |

recall 60% 미만: **전갱이 57.1%, 숭어 51.3%** (2종뿐)

### 기각된 가설 2개 — 여기가 이 로그의 핵심이다

**① "데이터가 적은 종이 무너질 것이다" → 틀렸다.**
최소 장수인 볼락(146)·삼치(151)는 하위 8종에 없다. 삼치는 recall 81.0%로 오히려 상위권이다.
장수와 recall의 상관이 약하다 → **재수집은 최우선 수단이 아니다.**

**② "혼동 쌍 6그룹이 주범일 것이다" → 틀렸다.**
틀린 예측이 짝으로 가지 않고 그룹 밖으로 흩어진다:

| 종 | recall | 짝으로 | **그룹 밖으로** |
|---|---|---|---|
| 숭어 | 51.3% | 농어 2.6% | **46.2%** |
| 메기 | 61.4% | 동자개 0.0% | **38.6%** |
| 전갱이 | 57.1% | 고등어·삼치 7.2% | **35.7%** |
| 벵에돔 | 66.7% | 감성돔 5.6% | **27.8%** |
| 동자개 | 72.7% | 메기 0.0% | **27.3%** |

메기↔동자개, 숭어↔농어, 고등어↔전갱이의 상호 혼동은 **0%에 가깝다**.
Phase 2에서 혼동 쌍 1,370장을 검수한 투자는 이미 회수됐다 —
붕어 93.5%, 농어 80.4%, 고등어 81.2%, 삼치 81.0%가 그 증거다.

예외(비대칭, 라벨 오염 의심): 잉어→붕어 11.4%, 볼락→우럭 18.2%.

### 남은 가설

에러가 특정 종으로 쏠리지 않고 흩어진다 = 체계적 혼동이 아니다. 후보는 둘:

1. **라벨 오염** — 웹 크롤링분에 무작위로 다른 종이 섞임
2. **식별 불가능한 사진** — 물고기가 너무 작거나 흐리거나 위장돼 사람도 못 맞추는 사진.
   `reports/aug_preview.jpg` 16장 중 3장(19%)이 이 부류였다(원거리 수중샷·위장 광어).
   iNat 관찰 사진에 흔하다. → 이거면 **수집이 아니라 품질 필터**가 답이다.

→ `reports/worst_cases/` 50장 육안 분류로 판별 중. (A)라벨오류 / (B)식별불가 / (C)모델미학습

---

## 실험 로그 (val 1,048장 고정 — 세 실험 모두 같은 시험지)

| # | 조건 | val Top-3 | 판정 |
|---|---|---|---|
| 1 | 베이스라인 (필터·mixup 없음) | **0.8721** | 기준선 |
| 2 | + mixup/cutmix | 0.8674 | ❌ 개선 없음 |
| A | + 품질필터 0.05 (train −10%) | 0.851 | ❌ 악화 |
| B | + 품질필터 0.30 (train −20.9%) | 0.8397 | ❌ 더 악화 |
| 5 | **입력 320px** (필터·mixup 없음) | **0.8807** | ✅ 첫 개선 +0.9pp |
| 6 | **입력 384px** | **0.8969** | ✅ **+2.5pp, 목표까지 0.3pp** |

### 기각된 가설 ③ — 정규화(mixup/cutmix)

과적합(train 0.99 vs val 0.75, 간격 24pp)을 줄이면 오를 것으로 봤다.
간격은 실제로 줄었으나 val은 오히려 0.005 낮아졌다.
→ **과적합은 증상이지 병목이 아니었다.** 간격이 아니라 val_top3만 보고 판단할 것.

### 기각된 가설 ④ — 사진 품질 필터

`quality_audit` 에서 상관이 뚜렷했다(하위 20% Top-3 77% vs 상위 20% 96%).
그런데 필터를 세게 걸수록 단조 감소했다. 원인은 둘이다:

**(1) 상관을 잘못 읽었다.** audit이 보여준 것은 "흐릿한 사진은 **맞히기** 어렵다"
(시험 난이도)인데, 이를 "흐릿한 사진은 **학습에 방해된다**"(학습 가치)로 넘겨짚었다.
둘은 다르다. 흐릿한 사진도 체형·지느러미 위치 같은 신호는 담고 있어서,
제거는 노이즈 제거가 아니라 그냥 데이터 손실이었다.

**(2) 균일 임계값이 소수 클래스를 골라 무너뜨렸다.**

| | 클래스별 최소 | 최대 | 불균형비 |
|---|---|---|---|
| 베이스라인 | 102장 | 246장 | 2.4x |
| B (0.30) | **39장** | 222장 | **5.7x** |

최대치는 10% 줄었는데 최소치는 62% 줄었다. 102장이던 볼락은 iNat 수중 사진
위주라 fish_prob가 낮고, **가장 여유 없는 종이 가장 많이 잘렸다.**
`score_quality.py --summary` 의 종별 제외 비율을 먼저 봤어야 했다.

→ 코드는 남겨둔다(`--min-fish-prob`, 기본 None=꺼짐). 다만 재시도한다면
   균일 임계값이 아니라 **종별 하한 장수를 보장하는 방식**이어야 한다.

### 다음 가설 ⑤ — 입력 해상도

val_top3가 0.84~0.87에 갇혀 있다. 정규화·데이터 필터링 모두 안 움직였으니
아직 안 건드린 구조적 요인을 본다. 유력한 후보는 **224px 입력**이다.

구분 단서가 미세하다 — 잉어의 입가 수염 2쌍, 전갱이의 꼬리 쪽 모비늘,
동자개의 검은 반점. 224px로 줄이면 이런 세부가 뭉개지고 체형·색만 남는다.

A에서 **top1은 올랐는데(0.7538→0.760) top3는 떨어진 것**도 이 해석과 맞는다.
큰 특징으로 1순위를 찍는 건 되는데, 미세 단서로 후보를 좁히는 건 안 되고 있다.

수집 사진의 중앙값이 긴 변 500px이라 320px 입력은 데이터가 받쳐준다.

```bash
python -m src.train --img-size 320 --batch-size 32 --num-workers 2 --no-mixup
```

### ✅ 채택된 가설 ⑤ — 입력 해상도 (2026-08-18)

224px → **320px 하나만 바꿔** val_top3 0.8721 → **0.8807**. 5개 실험 만의 첫 개선이다.

| | val top1 | val top3 | best epoch | 소요 |
|---|---|---|---|---|
| 224px | 0.7538 | 0.8721 | E18 | 16.9분 |
| 320px | **0.774** | **0.8807** | **E9** | 15.0분 |

**top1·top3가 함께 올랐다.** 품질필터 실험에서는 top1만 오르고 top3가 떨어졌는데
(큰 특징으로 1순위는 찍지만 미세 단서로 후보를 못 좁힘), 해상도를 올리자 둘 다 올랐다.
"224px에서 수염·모비늘·반점이 뭉개진다"는 해석과 일치한다.

**최고점이 E18 → E9로 당겨졌고 총 시간도 짧아졌다.** 정보가 많아지니 수렴이 빠르다.
해상도를 올리면 느려질 거라는 예상과 반대였다 — 조기종료가 더 일찍 걸렸기 때문이다.

#### 왜 이 가설만 통했나

앞선 넷은 전부 **"데이터를 어떻게 다룰까"**(양·검수·정규화·필터링)였다.
이건 **"모델에게 무엇을 보여줄까"**다. 전처리에서 정보가 물리적으로 사라지고 있었으므로,
데이터를 아무리 손봐도 움직이지 않았던 것이다.

#### 상한 주의

수집 사진의 긴 변 중앙값이 500px, 800px 초과는 상위 10%뿐이다.
384px면 원본을 거의 그대로 쓰는 셈이라 **그 이상은 없는 정보를 늘려 보는 것**이다.
448px 이상은 효과를 기대하기 어렵고, 실측으로 확인할 것.

### 해상도 스윕 결과 (2026-08-18)

| 해상도 | val top1 | val top3 | best epoch | 종료 사유 | 소요 |
|---|---|---|---|---|---|
| 224px | 0.7538 | 0.8721 | E18 | 조기종료 E26 | 16.9분 |
| 320px | 0.774 | 0.8807 | E9 | 조기종료 E17 | 15.0분 |
| **384px** | **0.813** | **0.8969** | **E26** | **epoch 소진(30/30)** | 33.3분 |

**384px는 조기종료가 걸리지 않았다.** 이전 실험은 전부 patience 8에 걸려 중단됐는데,
이번엔 30 epoch을 다 쓰고 끝났다 — 수렴해서가 아니라 **횟수가 부족해서** 멈춘 것이다.
E30 시점 lr이 2.09e-06까지 떨어져 더 못 움직였을 뿐이다.

→ `--epochs 50` 으로 늘리면 더 오를 여지가 있다. **이것이 다음 실험이다.**

Top-1이 0.7538 → 0.813 으로 6pp 가까이 올랐다. 해상도가 이 과제의 핵심 축이었다.

#### 아직 남은 별도 문제

`기타` recall 65.79%(기준 80%)는 해상도로 해결될 성질이 아닐 수 있다.
249장으로 "24종이 아닌 모든 것"을 커버하려는 구조 자체의 문제다.
전체 Top-3에 미치는 영향은 +0.5pp 정도지만 **합격 기준의 독립 항목**이므로 따로 다룬다.

---

## 🎯 합격선 도달 (2026-08-18, test 1,047장 / 384px `exp_384.pt`)

**전체 Top-3 90.16% — 기준 90% 통과.** 기준선 87.68%에서 +2.5pp.

| 지표 | 기준선(224px) | **384px** | 기준 | |
|---|---|---|---|---|
| 전체 Top-3 | 87.68% | **90.16%** | 90% | ✅ |
| 전체 Top-1 | 76.98% | **80.99%** | 75% | ✅ |
| 어종 24종 Top-3 | 88.01% | **90.49%** | 90% | ✅ |
| `기타` 오탐율 | 0.99% | 0.79% | 10%↓ | ✅ |
| **`기타` recall** | 65.79% | **71.05%** | 80% | ❌ |
| **종별 recall 하한** | 숭어 51.3% | **볼락 50.0% / 숭어 59.0%** | 60% | ❌ |
| **easy종 Top-1** | — | **메기 75.0%** | 90% | ❌ |
| **hard종 Top-3** | — | **잉어 88.6% / 볼락 86.4%** | 90% | ❌ |

### 아직 완료로 볼 수 없는 이유

**여유가 0.16pp뿐이다.** test 1,047장에서 사진 2장 차이로 뒤집힌다.
종별 기준 3개도 미달이므로 마진을 벌고 개별 항목을 잡아야 한다.

### 종별 수치를 읽을 때 주의

볼락 test 표본은 **22장**이다. `recall 50.0%` = 11장 정답이고, 한 장만 더 맞히면
54.5%가 된다. 표본이 작은 종(볼락 22, 전갱이 28, 벵에돔 36, 기타 38)의 recall은
오차 범위가 넓으니 **한두 장 차이에 반응해 방향을 바꾸지 말 것.**

반면 `기타` recall 71.05%는 기준선 65.79%에서 개선폭이 작다. 해상도로 풀리는
성질이 아니며, **249장으로 "24종이 아닌 모든 것"을 커버하는 구조 자체의 문제**다.

### 남은 작업 (우선순위 순)

1. **`--epochs 50`** — 384px가 횟수 소진으로 끝났으므로 마진 확보 여지가 있다
2. **TTA(좌우반전 평균)** — 재학습 없이 +0.5~1.5pp 기대. val에서 튜닝할 것
3. **`기타` 보강** — 독립 항목. 버킷 다양성이 관건이지 장수만 늘리는 게 아니다
4. 볼락·숭어 — 표본이 작아 우선순위 낮음. 1~2로 함께 오를 수 있다

### ⚠️ test셋 사용 원칙

`evaluation.md` "하지 말 것"대로 **test를 보고 하이퍼파라미터를 고르지 않는다.**
지금까지 test는 2회 사용했다(기준선 1회, 384px 1회). 이후 실험 선택은 전부
val 기준으로 하고, test는 최종 확인에만 쓴다.

---

### 기각된 가설 ⑥ — "epoch을 늘리면 더 오른다" (2026-08-18)

384px 30 epoch 실험이 조기종료 없이 끝났기에 `--epochs 50` 을 시도했다.
**결과 0.8874 로 30 epoch(0.8969)보다 낮았다.**

원인은 **코사인 학습률 스케줄이 총 epoch 수에 맞춰 늘어난다**는 점이다:

| | E20 시점 lr | 결과 |
|---|---|---|
| 30 epoch | 1.15e-04 | E26에 2.75e-05까지 내려가며 **0.8969** |
| 50 epoch | **2.28e-04** | 그 구간에 닿기 전 E20에서 조기종료(patience 8) → 0.8874 |

30 epoch 실험 후반의 상승은 **횟수가 늘어서가 아니라 학습률이 거의 0까지 떨어져
미세 조정이 됐기 때문**이었다. 실제로 그 로그의 마지막 구간
(E25~E30: 0.891 / 0.897 / 0.892 / 0.889 / 0.895 / 0.896)은 이미 평평하다.
**"조기종료가 안 걸렸다 = 덜 수렴했다"는 읽기가 틀렸다.**

→ **30 epoch 스케줄이 이 문제 크기에 맞다.** 굳이 늘리려면 patience도 함께
   키워야 하지만(예: 15), 위 평탄 구간을 보면 기대 이득이 작다.
   `--epochs 30` 을 기본으로 유지한다.

---

## ✅ `기타` 보강 + 층화 분할 결과 (2026-08-18, val 1,117장)

`기타` 249 → **706장** 수집(iNat 24종밖 어종 +280, 검색 비물고기 +180),
출처별 층화 분할 적용. 전체 6,999 → **7,456장**.
다른 24종의 split 배정은 **한 장도 바뀌지 않았다**(종별 독립 시드).

| 지표 | 이전(val 1,048) | **현재(val 1,117)** | 기준 | |
|---|---|---|---|---|
| 전체 Top-3 | 89.69% | **91.05%** | 90% | ✅ |
| 전체 Top-1 | 81.20% | **81.65%** | 75% | ✅ |
| **`기타` recall** | **43.2%** | **86.79%** | 80% | ✅ |
| `기타` 오탐율 | 0.99% | 3.26% | 10%↓ | ✅ |
| 어종 24종 Top-3 | 90.80% | 90.41% | 90% | ✅ |
| 종별 recall 하한 | 기타 43.2% | 최저 숭어 62.5% | 60% | ✅ |

**recall 60% 미만 종이 0개다.** val이 더 어려워졌는데도(`기타` 37→106장,
어종 비율 53%로 통일) 점수가 올랐다.

### 대가는 예상 범위

`기타` 오탐율 0.99% → 3.26%, 어종 24종 Top-3 90.80% → 90.41%(-0.4pp).
`기타`를 많이 가르쳤으니 물고기를 `기타`로 미는 일이 늘어난 것이다.
기준 10%에 한참 못 미치므로 감수할 만하다.

### 아직 미달인 2개 — 기준 자체를 재검토할 것

| 기준 | 현황 |
|---|---|
| easy 종 Top-1 ≥ 90% | 메기 67.4%, 송어 69.2%, 블루길 71.4%, 배스 71.7% |
| hard 종 Top-3 ≥ 90% | 볼락 81.8% |

**easy 기준(90%)은 계획 단계에서 낙관적으로 잡은 값으로 보인다.** 전체 Top-1이
81.65%인데 특정 종에만 90%를 요구하는 것은 정합성이 없다. 게다가 `difficulty`
필드는 데이터가 아니라 계획서에서 사람이 붙인 라벨이며, 실측상 배스·블루길·송어가
서로 헷갈린다 — "easy"라는 분류 자체가 틀렸다.

→ 성능을 끌어올리기 전에 [evaluation.md](evaluation.md) 합격 기준을 실측 기반으로
   재조정할지 먼저 결정할 것. 숫자를 맞추려 기준을 낮추는 것과, 처음부터 근거 없이
   잡힌 기준을 바로잡는 것은 다르다. **후자임을 근거와 함께 남길 것.**
