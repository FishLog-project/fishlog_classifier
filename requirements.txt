# fish-classifier — 개발/학습 전체 의존성
# Python 3.11 또는 3.12 권장 (3.14는 torch/albumentations 휠 미비)
#
# GPU 학습 시 torch 는 아래처럼 CUDA 빌드로 따로 설치할 것:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
# (PyPI 기본 휠은 Windows에서 CPU 전용)

# --- 학습 ---
torch>=2.4
torchvision>=0.19
timm>=1.0.9
albumentations>=2.0        # 2.x API 기준으로 dataset.py 작성됨
# albumentations→albucore 하위 의존성. 5.1.2는 Windows cp311 휠이 없어 소스 빌드(MSVC 14+)를
# 요구한다 → 휠이 있는 5.1.1로 고정. 상위 버전이 휠을 내면 이 핀은 지워도 된다.
stringzilla==5.1.1
opencv-python>=4.10
pillow>=10.3
numpy>=1.26

# --- 평가/시각화 ---
scikit-learn>=1.5
matplotlib>=3.9
pandas>=2.2
tqdm>=4.66

# --- 서빙 (배포 이미지는 server/requirements.txt 사용) ---
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
onnx>=1.16
onnxruntime>=1.19
# torch 2.9+ 의 dynamo ONNX 익스포터가 요구한다. 없으면 export가 ModuleNotFoundError로 죽는다.
onnxscript>=0.7

# --- Phase 2 데이터 수집 ---
requests>=2.32
# fastapi TestClient 가 내부적으로 요구한다 (scripts/check_server.py).
# 지금까지는 다른 패키지에 딸려 들어와 우연히 동작했다 → 명시적으로 선언한다.
httpx>=0.27
icrawler                   # Bing 이미지 크롤링 (scripts/crawl_search.py)
ddgs                       # DuckDuckGo 이미지 검색 (scripts/crawl_ddg.py) — 한국 낚시 사진 주 소스>=0.6.9
imagehash>=4.3
